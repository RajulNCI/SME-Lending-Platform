# CreditCore Web — copy to .env.local, never commit real values

VITE_API_BASE_URL=http://localhost:8000

# AWS Cognito — required for real login
# Leave blank to use demo mode (borrower@company.ie / demo)
VITE_COGNITO_USER_POOL_ID=
VITE_COGNITO_CLIENT_ID=
VITE_COGNITO_REGION=us-east-1

VITE_ENABLE_AI_ACT_PANEL=true
VITE_SENTRY_DSN=
